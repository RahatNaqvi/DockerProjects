version: '3'

services:
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: testdb

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
